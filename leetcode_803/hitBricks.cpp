#include <iostream>
#include <queue>
#include <vector>
#include <set>

using std::vector;
using std::set;
using std::queue;

class Solution {
    void buildDependencyByBfs(int i, int j, int M, int N, vector<set<int>>& upstream) {
        vector<vector<int>> mark(M, vector<int>(0, N));
        
    } 
public:
    vector<int> hitBricks(vector<vector<int>>& grid, vector<vector<int>>& hits) {

    }
};